name = nexus
description = Cross-server gate travel system. Provides gate-to-gate travel, state synchronization, anti-duplication, and a power interface for any energy mod to plug into. Runs behind mt-multiserver-proxy with the nexus_proxy Go plugin. Standalone — gates are free by default; set nexus.require_power=true to enforce power checks.
depends =
optional_depends =
author = ringworld
release = 1
