name = nexus
description = Cross-server zone travel system. Provides gate-to-gate travel, state synchronization, and extensible transfer hooks. Runs behind mt-multiserver-proxy with the nexus_proxy Go plugin.
depends =
optional_depends =
author = ringworld
release = 1
